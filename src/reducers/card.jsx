// import { CHANGE_INPUT_VALUE, SET_LOGIN, SET_LOGOUT } from '../actions/user';

export const initialState = {
	id: null,
	description: '',
	position: null,
};

const reducer = (state = initialState, action = {}) => {
	switch (action.type) {

		
		default:
			return state;
	}
};

export default reducer;